import os
import json
from datetime import datetime
def create_github_upload_guide():
    """创建GitHub上传指南"""
    print("🎯 创建GitHub上传指南...")
    
    project_dir = "ScreenPermissionManager_Test/ScreenPermissionManager_Test"
    
    # 创建上传脚本
    upload_guide = f"""# 🚀 GitHub上传指南 - 屏幕权限管理测试APP
## 📋 项目信息
- **项目名称**: screen-permission-manager-test
- **描述**: 屏幕权限管理测试APP - 完整Flutter应用
- **版本**: 1.0.0
- **创建时间**: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}
- **项目位置**: {os.path.abspath(project_dir)}
## ✅ 已完成的工作
- ✅ Flutter主程序 (lib/main.dart)
- ✅ 项目配置 (pubspec.yaml)
- ✅ Android配置 (AndroidManifest.xml)
- ✅ GitHub Actions自动构建 (.github/workflows/build.yml)
- ✅ 项目文档 (README.md)
## 🚀 立即上传步骤（3分钟）
### 第一步：创建GitHub仓库
1. 访问 **https://github.com/new**
2. 填写仓库信息：
   - **仓库名称**: `screen-permission-manager-test`
   - **描述**: `屏幕权限管理测试APP - 基于Flutter的完整功能演示`
   - **选择**: **公开** (推荐，便于分享)
   - **初始化**: **不要勾选** "Add a README file"
3. 点击 **"Create repository"**
### 第二步：获取推送命令
创建仓库后，GitHub会显示推送命令，类似：
```bash
git remote add origin https://github.com/YOUR_USERNAME/screen-permission-manager-test.git
git branch -M main
git push -u origin main
```
### 第三步：在当前环境中执行推送
让我帮您执行推送（需要您的GitHub用户名）：
```bash
cd {project_dir}
# 然后执行GitHub显示的命令
```
### 第四步：启用GitHub Actions
1. 访问您的仓库：`https://github.com/YOUR_USERNAME/screen-permission-manager-test`
2. 点击 **"Actions"** 标签页
3. 如果看到提示，点击 **"Enable workflow"**
4. 等待构建完成（约5-10分钟）
### 第五步：下载和安装APK
1. 构建完成后，进入 **"Actions"** 标签页
2. 点击最新的workflow run
3. 在 **"Artifacts"** 部分下载 `app-release`
4. 解压并安装到Android设备
## 📱 安装和测试
### 安装APK到手机
```bash
# 方法1: 使用ADB（推荐）
# 连接手机并开启USB调试后：
adb install app-release.apk
# 方法2: 手动安装
# 1. 将APK传输到手机
# 2. 在手机上点击安装
# 3. 按照提示完成
```
### 测试APP功能
1. **打开APP** - 看到蓝色主题的主界面
2. **点击右下角浮动按钮** - 模拟屏幕开关
   - 屏幕开启：绿色主题
   - 屏幕关闭：橙色主题
3. **开启自动管理开关** - 体验智能控制
4. **使用手动按钮** - 测试恢复/停止功能
5. **观察实时统计** - 查看数据变化
## 🎯 完整的测试流程
### 基础测试
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开APP | 看到主界面，状态为"等待测试" |
| 2 | 点击浮动按钮 | 界面变橙色，显示"屏幕已关闭" |
| 3 | 再次点击 | 界面变绿色，显示"屏幕已开启" |
### 高级测试
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 开启自动管理 | 开关变为蓝色 |
| 2 | 关闭屏幕 | 自动限制3个应用，显示"已限制3个应用" |
| 3 | 开启屏幕 | 自动恢复所有，显示"所有应用正常" |
| 4 | 点击"恢复权限" | 受管应用数变为0 |
| 5 | 点击"停止应用" | 受管应用数变为5 |
## 📊 功能演示
### 屏幕状态管理
- 🟢 **绿色主题**: 屏幕开启，所有应用正常
- 🟠 **橙色主题**: 屏幕关闭，限制后台应用
### 智能权限控制
```dart
// 核心逻辑
if (screenOn) {
  // 恢复所有应用权限
  _managedCount = 0;
  _status = '屏幕开启 - 所有应用正常';
} else {
  // 限制非必要应用
  _managedCount = 3;
  _status = '屏幕关闭 - 已限制3个应用';
}
```
### 实时统计显示
- **受管应用**: 当前限制的应用数量
- **屏幕状态**: 实时显示开/关状态
- **自动模式**: 显示自动管理是否启用
## 🔧 故障排除
### 推送代码失败
```bash
# 检查是否登录GitHub
git config --global user.name "您的用户名"
git config --global user.email "您的邮箱"
# 重新设置远程仓库
git remote set-url origin https://github.com/YOUR_USERNAME/screen-permission-manager-test.git
```
### Actions构建失败
1. 查看Actions日志获取详细错误
2. 检查项目文件是否完整
3. 确认pubspec.yaml格式正确
### 安装APK失败
1. **Android版本**: 确保≥5.0
2. **未知来源**: 设置→安全→未知来源应用：启用
3. **存储空间**: 检查手机剩余空间
## 📁 项目文件说明
```
ScreenPermissionManager_Test/
├── lib/
│   └── main.dart              # Flutter主程序（核心功能）
├── android/
│   └── app/src/main/
│       └── AndroidManifest.xml # Android配置
├── .github/
│   └── workflows/
│       └── build.yml          # 自动构建配置
├── pubspec.yaml               # 项目配置
└── README.md                  # 项目文档
```
### 核心文件详解
- **lib/main.dart**: 包含完整的APP逻辑，500+行代码
- **.github/workflows/build.yml**: 自动构建配置，每次推送自动构建
- **pubspec.yaml**: Flutter项目配置和依赖管理
## 🎯 成功标志
完成以上步骤后，您将获得：
- ✅ **GitHub项目仓库** - 可在线访问
- ✅ **自动构建系统** - 每次推送自动构建APK
- ✅ **APK下载链接** - 通过Actions下载
- ✅ **可分享的APP** - 可以分享给朋友
- ✅ **完整的文档** - 便于理解和维护
## 🆘 需要帮助？
### 如果您想让我帮您执行推送
请提供：
- **您的GitHub用户名**
- **仓库是否公开**（推荐公开）
### 如果您想自己操作
1. 按照上面的步骤创建仓库
2. 复制GitHub显示的推送命令
3. 在本工具中执行这些命令
### 如果遇到问题
- 查看本指南的故障排除部分
- 检查GitHub Actions日志
- 确认网络连接正常
## 📊 预计时间
| 步骤 | 时间 |
|------|------|
| 创建GitHub仓库 | 1分钟 |
| 推送代码 | 30秒 |
| GitHub Actions构建 | 5-10分钟 |
| 下载APK | 1-2分钟 |
| **总计** | **8-15分钟** |
## 🎉 完成后您将拥有
### 可立即使用的APP
- 📱 **可安装的APK文件**
- 🎨 **美观的Material Design界面**
- 🤖 **智能权限管理系统**
- 📊 **实时统计和日志**
### 可分享的项目
- 🌐 **GitHub上的完整仓库**
- 🔄 **自动构建系统**
- 📚 **完整文档**
- 🚀 **持续集成/持续部署**
## 📝 项目信息文件
本项目还包含 `project_info.json` 文件，包含：
```json
{{
  "name": "screen-permission-manager-test",
  "description": "屏幕权限管理测试APP",
  "version": "1.0.0",
  "status": "准备上传GitHub",
  "files_created": {len(['lib/main.dart', 'pubspec.yaml', 'android/...', '.github/...', 'README.md'])},
  "next_action": "上传到GitHub并触发构建"
}}
```
---
**准备时间**: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}
**状态**: 🟢 立即可用
**下一步**: 创建GitHub仓库并推送代码
## 🚀 立即开始
1. 访问 https://github.com/new
2. 创建仓库: `screen-permission-manager-test`
3. 获取推送命令
4. 让我帮您执行或自行推送
5. 等待5-10分钟，下载APK
6. 安装到手机测试！
**老板，您的APP已准备就绪，随时可以上传！(｡･ω･｡)ﾉ♡**
"""
    
    with open(f"{project_dir}/UPLOAD_GUIDE.md", "w", encoding="utf-8") as f:
        f.write(upload_guide)
    
    print("✅ 上传指南已创建")
    
    # 创建简易推送脚本
   简易推送脚本 = """#!/bin/bash
# 简易推送脚本
echo "🚀 GitHub推送助手"
echo "=================="
if [ -z "$1" ]; then
    echo "❌ 请提供GitHub用户名"
    echo "用法: ./simple_push.sh YOUR_USERNAME"
    exit 1
fi
USERNAME=$1
REPO_NAME="screen-permission-manager-test"
echo "📋 仓库: $USERNAME/$REPO_NAME"
echo ""
echo "步骤1: 在GitHub创建空仓库"
echo "  访问: https://github.com/new"
echo "  名称: $REPO_NAME"
echo "  不要初始化README"
echo ""
echo "步骤2: 复制下面的命令执行:"
echo "  git remote add origin https://github.com/$USERNAME/$REPO_NAME.git"
echo "  git branch -M main"
echo "  git push -u origin main"
echo ""
echo "步骤3: 等待5-10分钟，下载APK"
echo "=================="
"""
    
    with open(f"{project_dir}/simple_push.sh", "w", encoding="utf-8") as f:
        f.write(简易推送脚本)
    os.chmod(f"{project_dir}/simple_push.sh", 0o755)
    
    print("✅ 简易推送脚本已创建")
    
    # 显示项目信息
    print(f"\n🎉 项目准备完成！")
    print("=" * 70)
    print(f"📁 项目位置: {os.path.abspath(project_dir)}")
    print(f"📦 已创建文件:")
    for file in ["lib/main.dart", "pubspec.yaml", "android/app/src/main/AndroidManifest.xml", 
                 ".github/workflows/build.yml", "README.md", "UPLOAD_GUIDE.md", "simple_push.sh"]:
        print(f"  ✅ {file}")
    
    print(f"\n🎯 下一步操作:")
    print(f"  1. 访问: https://github.com/new")
    print(f"  2. 创建仓库: screen-permission-manager-test")
    print(f"  3. 运行: cd {project_dir}")
    print(f"  4. 执行推送命令（从GitHub获取）")
    print(f"  5. 等待构建完成")
    print(f"  6. 下载APK并安装")
    
    print(f"\n📖 详细指南: {project_dir}/UPLOAD_GUIDE.md")
    print(f"   或告诉我您的GitHub用户名，我帮您执行推送！")
    
    utils.set_state(success=True, 
                   result="项目已准备就绪，可立即上传GitHub",
                   project_path=os.path.abspath(project_dir),
                   upload_ready=True)
if __name__ == "__main__":
    create_github_upload_guide()